spring:
  application:
    name: Eureka_Consumer
  cloud:
    config:
      name: application # 配置文件的名字application-dev.yml的base
      profile: dev
      label: master # 分支
      uri: http://root:123@127.0.0.1:12580
